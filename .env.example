# Minimal environment overrides (optional)
# Redis URL (override if using external Redis)
REDIS_URL=redis://redis:6379/0

# Optional: custom config file path (TOML)
# APP_CONFIG_FILE=./server/config/app.toml
